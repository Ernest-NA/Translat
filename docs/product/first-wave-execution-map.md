# First-wave execution map

## Objective
Translate the earliest backlog work into repository-operable slices.

## First executable wave
### Foundation and bootstrap
Legacy refs:
- `A1` scaffold repository from ForgeSeed
- `A2` adapt AGENTS and define Git/release workflow
- `A3` create initial skill set
- `A4` align repository structure and workflow artifacts

### Technical foundation
Legacy refs:
- `B1` create Tauri shell
- `B4` integrate encrypted SQLite and migrations

### Early document flow
Legacy refs:
- `C1` CRUD projects
- `C2` document import
- `C3` normalization and segmentation

### Early terminology support
Legacy refs:
- `D1` glossary CRUD
- `D2` glossary entries and variants

### First AI baseline
Legacy refs:
- `E1` action registry and action definition
- `E2` base action orchestrator
- `E3` context builder
- `E4` model policy service
- `E5` estimate_cost
- `E6` translate_segment
- `E8` task run persistence

## Repo-oriented interpretation
These tasks should now be executable through:
- a task branch from `develop`
- a PR against `develop`
- Codex guided by `AGENTS.md` and `.agents/skills/`
- repository docs under `docs/`
- backlog traceability through canonical `TR-*` Notion task identifiers, preserving the legacy refs above as historical mappings

## Immediate next practical outcome
After the legacy `A4` slice, the repository should be ready to start implementing the technical foundation tasks without relying on a generic starter layout.
